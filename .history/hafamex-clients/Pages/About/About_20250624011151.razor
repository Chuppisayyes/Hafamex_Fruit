@page "/about"
@page "/ve-chung-toi"
@using Microsoft.JSInterop
@using Microsoft.AspNetCore.Components.Routing
@implements IDisposable
@inject IJSRuntime JS
<div class="wrapper d-flex flex-nowrap">
    <section
        class="section section--large hero flex-shrink-0 vw-100 vh-100 d-flex justify-content-center align-items-center">
        <div class="hero-banner">

        </div>
        <div class="container">
            <div class="hero-content">
                <h1 class="hero-title">
                    From Farm to <br><span class="highlight">Global Market</span>
                </h1>
                <p class="hero-subtitle">
                    Premium Vietnamese agricultural products exported worldwide with international quality standards
                </p>
                <div class="hero-buttons">
                    <button class="btn-view-products">
                        <a href="/product">View Our Products</a>
                    </button>
                    <button class="btn-learn-more">
                        <NavLink href="#">Learn More</NavLink>
                    </button>
                </div>
            </div>
        </div>
    </section>
    <section
        class="section section--dark section--small vh-100 flex-shrink-0 d-flex justify-content-center align-items-center">
        Part Two
    </section>
    <section class="section section--small vh-100 flex-shrink-0 d-flex justify-content-center align-items-center">
        Part Three
    </section>
    <section
        class="section section--large flex-shrink-0 vw-100 vh-100 d-flex justify-content-center align-items-center four">
        Part Four
    </section>
</div>

<nav>
    <div>one</div>
    <div>two</div>
    <div>three</div>
    <div>four</div>
</nav>

@* @code {
    private async Task TestLogAsync()
    {
        await JS.InvokeVoidAsync("console.log", "ScrollSection.init được gọi");
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await Task.Delay(300);
            await JS.InvokeVoidAsync("alert", "OnAfterRenderAsync đã chạy");
            await TestLogAsync(); // in log ra browser
            await JS.InvokeVoidAsync("scrollInterop.init");
        }
    }
} *@
@code {
    public About()
    {
        Console.WriteLine("About component constructor đã được gọi");
    }

    public void Dispose()
    {
        Console.WriteLine("About component Dispose được gọi");
    }

    protected override void OnInitialized()
    {
        Console.WriteLine("About OnInitialized được gọi");
    }

    protected override void OnAfterRender(bool firstRender)
    {
        Console.WriteLine($"OnAfterRender chạy với firstRender = {firstRender}");
    }
}